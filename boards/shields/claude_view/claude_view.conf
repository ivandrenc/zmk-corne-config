CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_BLANK_ON_IDLE=n
# Enable keymap event compilation on the peripheral so that
# zmk_layer_state_changed (forwarded from central over BLE) is
# available for the display to subscribe to.
CONFIG_ZMK_KEYMAP=y
