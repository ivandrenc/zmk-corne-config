description: |
  Select a display animation by index.
  Use as:  &anim_select 0   (wave)
           &anim_select 1   (gym)

compatible: "zmk,behavior-anim-select"

properties:
  "#binding-cells":
    type: int
    const: 1
